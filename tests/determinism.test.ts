// MANDATORY: must pass before any physics change merges (CLAUDE.md).
// 1) same seed + same trace, run twice => identical stateHash
// 2) golden master: recorded fixture trace => known hash
import { describe, it } from "vitest";
describe.todo("raceSim determinism — implement in Phase 0 prompt 0.5");
