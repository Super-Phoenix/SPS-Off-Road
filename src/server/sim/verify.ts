// Headless re-simulation harness — THE integrity boundary (BUILD_SPEC §2.2).
// Imports raceSim from @game/systems (architectural proof of purity).
// verify(seed, traces, loadouts) -> RaceResult; compare to claimed result.
export {}; // TODO(phase-1): implement per prompts/phase-1-solo-loop.md §1.5
