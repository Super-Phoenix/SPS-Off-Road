// Orchestrates a full deterministic race. Implemented in Phase 0/1 prompts.
// MUST be importable by src/server/sim/verify.ts with no browser globals.
// 60Hz fixed timestep. Same seed + same InputTrace[] => identical RaceResult.
export {}; // TODO(phase-0): implement per prompts/phase-0-foundations.md §0.5
